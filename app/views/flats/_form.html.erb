<!-- app/views/flats/_form.html.erb -->

<%= form_with model: flat, local: true do |form| %>
  <div class="form-group">
    <%= form.label :name, 'Nombre del Apartamento' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <!-- Agrega campos adicionales según tus necesidades (description, price_per_night, number_of_guests, address) -->

  <div class="form-group">
    <%= form.submit 'Guardar', class: 'btn btn-primary' %>
  </div>
<% end %>
